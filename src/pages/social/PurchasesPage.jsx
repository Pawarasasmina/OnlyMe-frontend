import{Link}from"react-router-dom";import{useQuery}from"@tanstack/react-query";import{walletService}from"../../services/walletService";export default function PurchasesPage(){const q=useQuery({queryKey:["world-entitlements"],queryFn:()=>walletService.getWorldEntitlements().then(r=>r.data.data.items),retry:false});return <div><h1 className="text-3xl font-black">Purchased Worlds</h1><p className="mt-2 text-sm text-atseen-muted">One-time World purchase history from your authenticated account.</p>{q.isLoading?<p className="mt-6">Loading purchases…</p>:q.isError?<p className="mt-6 text-red-300">Purchase history is unavailable.</p>:q.data.length?<div className="mt-6 grid gap-4 sm:grid-cols-2">{q.data.map(x=><article className="overflow-hidden rounded-2xl border border-atseen-line bg-atseen-surface" key={x.id}>{x.publication?.coverMedia?.secureUrl?<img alt="" className="aspect-video w-full object-cover" src={x.publication.coverMedia.secureUrl}/>:null}<div className="p-4"><h2 className="font-black">{x.publication?.title||"World unavailable"}</h2><p className="mt-1 text-xs text-atseen-muted">Paid ✦{x.purchasedStars} · {new Date(x.grantedAt).toLocaleDateString()}</p><p className={`mt-2 text-sm font-bold ${x.status==="ACTIVE"?"text-emerald-300":"text-red-300"}`}>{x.status}</p>{x.status==="ACTIVE"&&x.publication?<Link className="mt-4 inline-block rounded-full bg-atseen-blue px-4 py-2 text-xs font-bold text-atseen-bg" to={`/world/${x.publication._id||x.publication.id}`}>Open World</Link>:<p className="mt-3 text-xs text-atseen-muted">Access is no longer active. Purchase history is retained.</p>}</div></article>)}</div>:<p className="mt-6 rounded-2xl border border-dashed border-atseen-line p-10 text-center text-atseen-muted">No World purchases yet.</p>}</div>}
