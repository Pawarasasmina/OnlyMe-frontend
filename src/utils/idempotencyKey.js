export function createIdempotencyKey(prefix="financial"){if(globalThis.crypto?.randomUUID)return`${prefix}:${globalThis.crypto.randomUUID()}`;const bytes=new Uint8Array(16);globalThis.crypto?.getRandomValues?.(bytes);const random=[...bytes].map(x=>x.toString(16).padStart(2,"0")).join("")||`${Math.random().toString(16).slice(2)}${Math.random().toString(16).slice(2)}`;return`${prefix}:${random.slice(0,32).padEnd(32,"0")}`}
