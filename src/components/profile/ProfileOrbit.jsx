import{Link}from"react-router-dom";import PlanetSlot from"./PlanetSlot";export default function ProfileOrbit({capabilities,planets=[],role}){if(role!=="creator"||(!planets.length&&!capabilities.isOwner))return null;const bySlot=Object.fromEntries(planets.map(p=>[p.planet?.slot,p]));return <section className="mt-5 overflow-hidden rounded-[22px] border border-atseen-line bg-[radial-gradient(70%_90%_at_50%_45%,#101b2c,#06080b_75%)] p-5"><div className="flex items-center justify-between"><div><h2 className="text-sm font-bold">Planet orbit</h2><p className="text-xs text-atseen-muted">Worlds live only on this creator profile.</p></div>{capabilities.isOwner?<Link className="text-xs font-bold text-atseen-blue" to="/studio/worlds">Manage Worlds</Link>:null}</div><div className="mx-auto mt-5 grid max-w-lg grid-cols-1 gap-4 sm:grid-cols-3"><PlanetSlot owner={capabilities.isOwner} planet={bySlot.WORLD_1}/><PlanetSlot owner={capabilities.isOwner} planet={bySlot.WORLD_2}/><PlanetSlot owner={capabilities.isOwner} planet={bySlot.PREMIUM} premium/></div></section>}
