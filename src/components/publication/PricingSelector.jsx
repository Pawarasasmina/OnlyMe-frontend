import{PREMIUM_PRESETS}from"../../utils/worldValidation";export default function PricingSelector({kind,pricing,onChange,disabled}){if(kind==="PREMIUM_WORLD")return <fieldset disabled={disabled}><legend className="font-black">Monthly price</legend><div className="mt-3 flex gap-2">{PREMIUM_PRESETS.map(n=><button className={`rounded-full border px-4 py-2 ${pricing?.starsAmount===n?"border-atseen-blue bg-atseen-blue/10":"border-atseen-line"}`} key={n} onClick={()=>onChange({mode:"MONTHLY",starsAmount:n,presetId:`MONTHLY_${n}`})} type="button">✦{n}/month</button>)}</div><p className="mt-3 text-xs text-atseen-muted">Membership features activate only when payments and entitlements are enabled.</p></fieldset>;return <label className="block font-black">One-time Stars price<input className="mt-2 w-full rounded-xl border border-atseen-line bg-atseen-bg p-3" disabled={disabled} min="1" onChange={e=>onChange({mode:"ONE_TIME",starsAmount:Number(e.target.value),presetId:null})} step="1" type="number" value={pricing?.starsAmount||1}/><span className="mt-2 block text-xs font-normal text-atseen-muted">Unlock once — yours forever. Purchases are not enabled yet.</span></label>}
