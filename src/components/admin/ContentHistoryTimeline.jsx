import AdminContentStatusBadge from "./AdminContentStatusBadge";
export default function ContentHistoryTimeline({ history = [] }) { if (!history.length) return <p className="rounded-xl border border-dashed border-slate-300 p-6 text-center text-sm text-slate-500">No content history is available.</p>; return <ol className="relative space-y-5 border-l-2 border-slate-200 pl-6">{history.map((event) => <li className="relative" key={event._id || event.transitionId}><span className="absolute -left-[1.95rem] top-1 h-3 w-3 rounded-full border-2 border-white bg-orange-500 ring-2 ring-slate-200" /><div className="rounded-xl border border-slate-200 bg-slate-50 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><p className="font-bold">{event.action?.replaceAll("_", " ")}</p><time className="text-xs text-slate-400">{new Date(event.createdAt).toLocaleString()}</time></div><div className="mt-3 flex items-center gap-2"><AdminContentStatusBadge status={event.previousStatus} /><span className="text-slate-400">→</span><AdminContentStatusBadge status={event.newStatus} /></div>{event.admin ? <p className="mt-3 text-xs text-slate-500">Reviewer: {event.admin.name || event.admin.username || event.admin.email || event.admin}</p> : null}{event.creatorVisibleMessage ? <p className="mt-3 text-sm"><strong>Creator message:</strong> {event.creatorVisibleMessage}</p> : null}{event.internalNote ? <p className="mt-2 rounded-lg bg-slate-200/60 p-3 text-sm"><strong>Internal note:</strong> {event.internalNote}</p> : null}{event.reasonCodes?.length ? <p className="mt-2 text-sm text-red-700"><strong>Reasons:</strong> {event.reasonCodes.join(", ").replaceAll("_", " ")}</p> : null}{event.transitionId ? <p className="mt-3 break-all font-mono text-[10px] text-slate-400">Transition {event.transitionId}</p> : null}</div></li>)}</ol>; }
