import { ACCESS_LEVELS, formatContentLabel } from "../../utils/content";
export default function AccessLevelSelector({ value, coinPrice, onChange, onCoinPriceChange }) { return <div className="space-y-3">{ACCESS_LEVELS.map((level) => <label className={`block cursor-pointer rounded-2xl border p-4 ${value === level ? "border-brand-primary bg-brand-primary/10" : "border-white/10 bg-white/5"}`} key={level}><span className="flex items-center gap-3"><input checked={value === level} name="access" onChange={() => onChange(level)} type="radio" /><strong>{formatContentLabel(level)}</strong></span>{level === "SUBSCRIBER_ONLY" && value === level ? <p className="mt-2 text-xs text-brand-mist/55">Subscription access enforcement will be enabled in a later phase.</p> : null}{level === "PAY_PER_VIEW" && value === level ? <div className="mt-3"><input className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3" min="1" onChange={(event) => onCoinPriceChange(event.target.value)} placeholder="Coin price" step="1" type="number" value={coinPrice} /><p className="mt-2 text-xs text-brand-mist/55">Coin purchase and unlock transactions will be enabled later.</p></div> : null}</label>)}</div>; }
