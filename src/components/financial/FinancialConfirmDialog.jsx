import{useEffect,useRef}from"react";export default function FinancialConfirmDialog({open,title,onClose,children,actions}){const ref=useRef(null),previous=useRef(null);useEffect(()=>{if(!open)return;previous.current=document.activeElement;const node=ref.current;node?.querySelector("button,input,textarea")?.focus();const key=e=>{if(e.key==="Escape")onClose();if(e.key==="Tab"){const items=[...node.querySelectorAll("button:not(:disabled),input:not(:disabled),textarea:not(:disabled),a[href]")];if(!items.length)return;const first=items[0],last=items.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}};document.addEventListener("keydown",key);return()=>{document.removeEventListener("keydown",key);previous.current?.focus?.()}},[open,onClose]);if(!open)return null;return <div aria-modal="true" className="fixed inset-0 z-[80] grid place-items-end bg-black/70 p-0 sm:place-items-center sm:p-4" role="dialog"><button aria-label="Close dialog" className="absolute inset-0 cursor-default" onClick={onClose} type="button"/><section className="relative max-h-[90vh] w-full overflow-y-auto rounded-t-3xl border border-atseen-line bg-atseen-surface p-5 text-atseen-text sm:max-w-lg sm:rounded-3xl" ref={ref}><div className="flex justify-between gap-4"><h2 className="text-xl font-black">{title}</h2><button aria-label="Close" onClick={onClose} type="button">✕</button></div><div className="mt-4">{children}</div><div className="mt-6 flex flex-col-reverse gap-2 sm:flex-row sm:justify-end">{actions}</div></section></div>}
